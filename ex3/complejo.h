#ifndef _COMPLEJO_H
#define _COMPLEJO_H

class Complejo{
  public:
    // Incluir un constructor por omisión y un constructor con parámetros.
    // Incluir las funciones get / set
    // Incluir los métodos para calcular magnitud y ángulo
    // Incluir un método para calcular el conjugado
    // Sobrecargar el operador de suma, resta, multiplicación
    // Sobrecargar el operador de flujo de salida para que imprima a + bj con el signo adecuado
};    

#endif









